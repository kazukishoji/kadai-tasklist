<ul>
<% tasks.each do |task| %>

    <li>
<%= link_to task.user.name, user_path(task.user) %>

<%= task.created_at %>

<%= task.status %>

<%= task.content %>
</li>

<% end %>

<%= paginate tasks %>

</ul>